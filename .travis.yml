dist: xenial

language: python

python:
  - 3.6

install:
  - pip install -r requirements-dev.txt -r requirements.txt

script:
  - pycodestyle app
  - pylint app
  - scripts/run.sh reload-db && pytest
