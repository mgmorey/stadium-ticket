[uwsgi]
enable-threads = true
gid = $(APP_GID)
http-socket = 0.0.0.0:$(APP_PORT)
manaage-script-name = true
master = true
mount = /=app:app
plugin = python3
socket = /tmp/$(APP_NAME).sock
thunder-lock = true
uid = $(APP_UID)
virtualenv = .venv
workers = 6
